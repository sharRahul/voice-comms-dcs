sounddevice>=0.4.6
vosk>=0.3.45
pyinstaller>=6.6.0

# Phase 2 local-first conversational cockpit dependencies
aiortc>=1.9.0
aiohttp>=3.13.4
websockets>=12.0
av>=12.0.0
numpy>=1.26.4
scipy>=1.13.0
requests>=2.32.0

# Phase 3 high-fidelity UX dependencies
pygame>=2.5.2
pynput>=1.7.6

# Optional Whisper.cpp Python binding:
#   python -m pip install -r requirements-whisper.txt -c constraints.txt
# The runtime falls back to whisper-cli when the binding is unavailable.
