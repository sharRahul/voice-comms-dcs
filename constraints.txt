# Exact constraints for reproducible local app/dev installs.
# Keep requirements.txt as the human-maintained runtime dependency list and install with:
#   python -m pip install -r requirements.txt -c constraints.txt
# Refresh these pins with tests plus pip-audit when dependencies are intentionally updated.

sounddevice==0.4.6
vosk==0.3.45
pyinstaller==6.6.0
aiortc==1.9.0
aiohttp==3.9.5
websockets==12.0
av==12.0.0
numpy==1.26.4
scipy==1.13.0
requests==2.32.0
pygame==2.5.2
pynput==1.7.6
whisper-cpp-python==0.2.0

# Development and audit tooling pins used by requirements-dev.txt.
pytest==8.3.5
pytest-cov==6.0.0
mypy==1.15.0
ruff==0.9.10
pip-audit==2.7.3
build==1.2.2
